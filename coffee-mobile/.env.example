# Supabase: скопируйте в .env.local (именно .env.local в папке coffee-mobile).
# Имена переменных — строго как ниже. Только anon key, не service_role.
# Где взять: Supabase Dashboard → Project Settings → API → Project URL и anon public key.

EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Backend URL для загрузки статуса заказа (GET /orders/:id).
# Production (Render): укажите URL вашего сервиса, например https://coffee-admin-xxxx.onrender.com
# Dev: можно не задавать — тогда используется LAN IP (localhost через 10.0.2.2 на Android эмуляторе).
# EXPO_PUBLIC_API_URL=https://your-app.onrender.com
